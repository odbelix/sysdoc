{% extends 'base.html.twig' %}

{% block body %}
    <h1>Questions list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Message</th>
                <th>Responsable_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for question in questions %}
            <tr>
                <td><a href="{{ path('admin_question_show', { 'id': question.id }) }}">{{ question.id }}</a></td>
                <td>{{ question.title }}</td>
                <td>{{ question.message }}</td>
                <td>{{ question.responsableid }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_question_show', { 'id': question.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_question_edit', { 'id': question.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_question_new') }}">Create a new question</a>
        </li>
    </ul>
{% endblock %}
