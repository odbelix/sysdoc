{% extends 'base.html.twig' %}

{% block body %}
{% include 'breadcrumb.html.twig' with {'breadcrumbs': breadcrumbs} %}
<div class="row">
    <div class="col-12">
    <h1>Edición</h1>
    <h3><a class="btn btn-info" href="{{ path('periodactivity_index', { 'id': calendar.id }) }}"><i class="fa fa-arrow-left"></i></a></h3>

    {{ form_start(edit_form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="form-group">
        {{ form_row(edit_form.startdate,{'attr': {'class':'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(edit_form.enddate,{'attr': {'class':'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(edit_form.description,{'attr': {'class':'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(edit_form.periodCalendar,{'attr': {'class':'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_row(edit_form.periodDuration,{'attr': {'class':'form-control'}}) }}
    </div>
        <input class="btn btn-success" type="submit" value="Editar" />
    {{ form_end(edit_form) }}
    <hr>
    {{ form_start(delete_form) }}
            <input class="btn btn-danger" type="submit" value="Eliminar">
    {{ form_end(delete_form) }}
    <hr>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script src="https://cdn.ckeditor.com/4.7.1/standard/ckeditor.js"></script>
<script>
CKEDITOR.replace( 'appbundle_periodactivity[description]' );
</script>
{% endblock %}
