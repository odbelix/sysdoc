{% extends 'base.html.twig' %}

{% block body %}
    <h1>Periodduration</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ periodDuration.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ periodDuration.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('periodduration_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('periodduration_edit', { 'id': periodDuration.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
